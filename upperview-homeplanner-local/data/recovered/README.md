# Recovered Data

Status: recovered.

This folder contains files downloaded from Wayback CDX/replay searches or original public asset URLs. The files are preserved for analysis and should not be deleted or overwritten.

Important notes:

- UpperView-specific dynamic PHP backend responses were not recovered.
- Some recovered `rendering.house/db/scripts/php/*.response` files are comparable backend responses for other clients. They are schema references only, not UpperView records.
- `focused-cdx-results.json`, when present, records focused endpoint searches and their results/errors.

